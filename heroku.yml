build:
  languages:
    - python
  packages:
    - libpq-dev
run:
  web: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 30 --max-requests 1000 wsgi:app
release:
  image: web
  command:
    - python populate_database.py